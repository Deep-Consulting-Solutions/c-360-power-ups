{
  "name": "c-360-trello-timer",
  "version": "1.0.0",
  "description": "Trello Power-Up for time tracking with task categories",
  "main": "public/index.html",
  "scripts": {
    "build": "node build-config.js",
    "dev": "npm run build && npx serve public -p 3000",
    "deploy": "npm run build && vercel --prod"
  },
  "keywords": [
    "trello",
    "power-up",
    "timer",
    "time-tracking"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "serve": "^14.2.0"
  }
}