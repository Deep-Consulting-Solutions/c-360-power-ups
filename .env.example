# Environment (staging or production)
# This determines which N8N webhook endpoint to use
ENVIRONMENT=staging

# N8N API Key
# Get this from your N8N instance settings
N8N_API_KEY=your-api-key-here

# Harvest API Configuration
# Get these from Harvest: Settings → Developers → Create Personal Access Token
HARVEST_ACCESS_TOKEN=your_harvest_personal_access_token
HARVEST_ACCOUNT_ID=your_harvest_account_id

# Trello Power-Up Settings (optional)
# You can find your Power-Up ID after registering it on Trello
POWER_UP_ID=your-power-up-id

# Vercel Deployment URL (update after first deployment)
VERCEL_URL=https://c-360-trello.vercel.app

# Trello to Harvest User Mappings (for multi-user timer support)
# Maps Trello usernames to Harvest user IDs
# Format: JSON object mapping Trello usernames to Harvest user IDs
# Example: TRELLO_HARVEST_USER_MAPPINGS={"johndoe":12345,"janedoe":12346}
# The system will first try to match users by email, then fall back to this mapping
# Leave empty if you want to rely only on email matching
TRELLO_HARVEST_USER_MAPPINGS={}

# ============================================
# How this works:
# ============================================
#
# The build script (build-config.js) automatically constructs the N8N URLs:
#
# For ENVIRONMENT=staging:
#   Start Timer: https://c360-staging-flows.app.n8n.cloud/webhook/staging/start-timer
#   Stop Timer:  https://c360-staging-flows.app.n8n.cloud/webhook/staging/stop-timer
#   Convert Checklist Items to Cards: https://c360-staging-flows.app.n8n.cloud/webhook/staging/create-child-cards
#
# For ENVIRONMENT=production:
#   Start Timer: https://c360-staging-flows.app.n8n.cloud/webhook/production/start-timer
#   Stop Timer:  https://c360-staging-flows.app.n8n.cloud/webhook/production/stop-timer
#   Convert Checklist Items to Cards: https://c360-staging-flows.app.n8n.cloud/webhook/production/create-child-cards
